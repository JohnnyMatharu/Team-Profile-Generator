//const Employee = require("./employee")

class Manager extends Employee{
    constructor (name, id, email)
    {
        super name id email 
        //in documentation syntax for repetitions
    this.name = name;
    this.id = id;
    this.email = email;
    //email
    return 

    }
    }
//link to index.js all 4 files to get data
   // module.export = Manager; 
    //export to index.html and then from there to html, or all these to pageTempelate connected to html final page
    //page tempelate - functions to return html, follow the module example 
    //testing for all 4 functions
    //make page for employee,manager,engineer and intern, and one pageTemplate page, one final html page
